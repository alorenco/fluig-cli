/**
 * Roda ao Salvar/Enviar a solicitação — e nos botões 💾/▶ do preview do
 * `fluigcli dev`. Lance `throw "mensagem"` para bloquear a gravação
 * (o portal exibe a mensagem como HTML).
 */
function validateForm(form) {
    // if (form.getValue("descricao") == "") {
    //     throw "Informe a <b>descrição</b>.";
    // }
}
