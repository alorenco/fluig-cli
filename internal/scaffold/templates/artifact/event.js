/**
 * Evento global: [[.Name]]
 *
 * Publicação: fluigcli event export events/[[.Name]].js
 *
 * O nome do arquivo (e da função) deve ser o id de um evento global que a
 * plataforma dispara (ex.: displayCustomThemes, beforeConvertViewToPDF).
 * Os parâmetros variam por evento — consulte a documentação do Fluig do
 * evento escolhido e ajuste a assinatura abaixo.
 */
function [[.Name]]() {

}
