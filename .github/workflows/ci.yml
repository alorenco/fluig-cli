name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: stable
      - name: Build
        run: go build ./...
      - name: Testes unitários
        run: go test ./...
      - name: Vet (inclui tags de integração)
        run: |
          go vet ./...
          go vet -tags=integration ./internal/fluig/
      - name: Lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest

  build-matrix:
    strategy:
      matrix:
        os: [windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v6
        with:
          go-version: stable
      - name: Build
        run: go build ./...
      - name: Testes unitários
        run: go test ./...
